{
  "name": "google-fit-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8383 --force",  // Explicitly bind to 0.0.0.0
    "build": "vite build",
    "serve": "vite preview --host 0.0.0.0 --port 8383"  // Also for preview mode
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.3"
  }
}
